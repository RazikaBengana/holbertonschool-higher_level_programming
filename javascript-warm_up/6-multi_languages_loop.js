#!/usr/bin/node
// This script logs a series of programming language statements to the console, in 3 lines,
// by using an array of string and a loop

const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < 3; i++) {
  console.log(lines[i]);
}
