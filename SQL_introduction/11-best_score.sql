-- Selects the score and name from the table named 'second_table'
SELECT score, name
FROM second_table

-- Filters to include only those rows where the score is 10 or higher
WHERE score >= 10

-- Orders the results by score in descending order, from highest to lowest
ORDER BY score DESC;
