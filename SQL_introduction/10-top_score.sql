-- Selects the 'score' and 'name' columns from the 'second_table'
-- Orders the result by 'score' in descending order, so the highest scores are listed first

SELECT score, name FROM second_table ORDER BY score DESC;
